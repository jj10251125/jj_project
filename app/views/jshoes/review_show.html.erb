<div class="container">
  <% if !flash[:alert].nil? %>
  <div class="alert alert-danger">
    <%= flash[:alert] %>
  </div>
  <% end %>

  <br>
  <br>
  <div class="row">
    <div class="col-md-1">
    </div>
    <div class="col-md-5 text-right">
      <img src="<%= @post.image_url(:medium) %>">
    </div>
    <div class="col-md-5">
      <p style="font-size:15px;"><%= @post.title %></p>
      <hr>
      <p><%= @post.content %></p>
      
    <hr>
    <% @post.comments.each do |comment| %>
    <p><strong>아이디</strong> <%= comment.content %> | <a href="/jshoes/delete_comment_complete/<%= comment.id %>">삭제</a></p>
    <% end %>
    <form action="/jshoes/review_comment_complete" method="post">
    <input type="hidden" name="review_id" value="<%= @post.id %>">
    <p>작성자 아이디: <input type="text" name="comment_content" placeholder="댓글 내용을 입력하세요."> <input type="submit" value="달기"></p>
    </form>
    <hr>
      <p><a href="/jshoes/review">전체 리스트 가기</a></p>
    </div>
    <div class="col-md-1">
    </div>
  </div>
</div>

